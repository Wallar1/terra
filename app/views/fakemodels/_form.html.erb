<%= form_with(model: fakemodel, local: true) do |form| %>
  <% if fakemodel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fakemodel.errors.count, "error") %> prohibited this fakemodel from being saved:</h2>

      <ul>
      <% fakemodel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
